apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chartTekton.fullname" . }}-test-connection"
  labels:
    {{- include "chartTekton.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chartTekton.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
